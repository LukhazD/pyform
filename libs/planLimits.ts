/**
 * Centralized plan limits — single source of truth.
 * Both plans (Pro Mensual & Pro Anual) share the same limits.
 */

/** Maximum completed responses allowed per form (lifetime, not monthly). */
export const RESPONSES_PER_FORM_LIMIT = 500;
